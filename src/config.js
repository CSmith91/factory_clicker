export const testMode = false; // unlocks everything if true
export const speedMode = false; // allows adding resources on request. This is used to speed up test gameplay (as opposed to the above, which is more for full development)
export const cheat = 0 // adds more storage by x (plus other things?)